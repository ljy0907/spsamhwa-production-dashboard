# PIN 접속 제한 로직 안내

정적 HTML(GitHub Pages 등, 백엔드 없음) 페이지 앞단에 PIN 번호 입력 화면을 두어,
PIN을 맞게 입력해야 실제 콘텐츠가 보이도록 만든 기능입니다. (`index.html`)

## 동작 방식

1. 페이지 로드 시 실제 콘텐츠(`#content`)는 숨기고, PIN 입력 화면(`#pin-gate`)만 노출.
2. 이미 인증한 적이 있으면 (`sessionStorage`에 플래그 존재) PIN 화면을 건너뛰고 바로 콘텐츠 노출.
3. 사용자가 PIN을 입력하면 브라우저 내장 Web Crypto API(`crypto.subtle.digest`)로 SHA-256 해시를 계산.
4. 계산된 해시값을 코드에 미리 넣어둔 `PIN_HASH` 상수와 비교.
   - 일치 → `sessionStorage`에 인증 플래그 저장 후 콘텐츠 표시
   - 불일치 → 에러 메시지 표시, 입력창 초기화
5. `sessionStorage` 특성상 인증 상태는 **같은 탭(세션) 안에서만 유지**되며, 탭을 닫으면 초기화됨.

## 왜 PIN 원문 대신 해시를 쓰는가

- 정적 페이지라 서버 검증이 불가능 → JS 코드 자체를 브라우저 개발자도구로 그대로 볼 수 있음.
- PIN 원문을 코드에 그대로 넣으면 소스만 봐도 즉시 노출됨.
- SHA-256 해시로 바꿔두면 최소한 코드를 봐도 PIN 원문이 바로 드러나지 않음.
- **다만 이는 완전한 보안이 아님.** 4자리 숫자 PIN은 해시가 있어도 오프라인 무차별 대입(경우의 수 10,000개)으로 몇 초 안에 뚫릴 수 있음.
- 즉 이 방식은 "진짜 보안"이 아니라 **가벼운 접근 차단(캐주얼 방문자 필터링)** 용도로만 사용해야 함.
- 진짜 보안이 필요하면 서버/인증 백엔드(예: Cloudflare Access, Netlify 비밀번호 보호, 별도 로그인 서버) 사용을 권장.

## PIN 변경 방법

터미널에서 새 PIN의 SHA-256 해시를 구한 뒤, `index.html`의 `PIN_HASH` 상수 값만 교체하면 됩니다.

```bash
printf '새로운PIN' | sha256sum
```

출력된 해시값(공백·개행 제외한 64자리 hex 문자열)을 아래 위치에 넣습니다.

```js
const PIN_HASH = "여기에_새_해시값";
```

## 관련 코드 위치

- 파일: `index.html`
- HTML: `#pin-gate` (입력 화면), `#content` (실제 콘텐츠)
- JS: `<script>` 블록 내 `PIN_HASH`, `SESSION_KEY`, `sha256()`, `tryUnlock()`

## 인증 유지 범위 변경하고 싶다면

- 탭을 닫아도 유지하려면: `sessionStorage` → `localStorage`로 교체
- 매번 새로 입력받고 싶다면: `sessionStorage` 체크/저장 로직 자체를 제거

---

## Claude Code에게 요청할 때 쓸 수 있는 프롬프트 예시

```
정적 HTML 페이지(index.html) 앞단에 PIN 번호 입력 게이트를 추가해줘.

요구사항:
- 페이지 로드 시 실제 콘텐츠는 숨기고 PIN 입력 화면만 보여줄 것
- PIN은 4자리 숫자, 값은 [PIN 값 지정]
- PIN 원문을 코드에 그대로 넣지 말고 SHA-256 해시로 비교할 것 (Web Crypto API 사용)
- 인증에 성공하면 sessionStorage에 플래그를 저장해서, 같은 탭에서는 재방문 시 다시 입력하지 않도록 할 것
- 이 페이지는 정적 페이지(백엔드 없음)라 완전한 보안이 아니라는 점을 알고 있고, 가벼운 접근 차단 용도로만 쓸 것임을 감안해서 구현해줘
```

필요에 따라 "인증 유지 범위"(탭 세션만 / 브라우저에 계속 유지 / 매번 입력)를 프롬프트에 명시하면 됩니다.
